import React from 'react';
import { Paper, Grid, Typography } from '@material-ui/core';

export default (props) =>
{

    return (

        <Paper>
            Potential Profit
        </Paper>
    );

}